rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // WARNING: This is a completely open rule. 
      // It allows anyone to read or write any file.
      // This is intended for debugging to resolve permission issues.
      allow read, write;
    }
  }
}
