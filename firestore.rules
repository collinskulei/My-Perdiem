rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to any authenticated user.
    // This is suitable for development but should be made more
    // secure before moving to production by adding role-based checks.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}